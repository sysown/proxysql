# Configuration file with invalid value ranges
# This tests value validation (e.g., port numbers, timeouts)

admin_variables =
{
    admin_credentials = "admin:admin;radmin:radmin"
    mysql_ifaces = "127.0.0.1:6032"
}

mysql_variables =
{
    threads = 4
    max_connections = 2048
    interfaces = "0.0.0.0:6033"
    default_schema = information_schema
}

mysql_servers =
(
    {
        address = "127.0.0.1"
        port = -1  # Invalid: port cannot be negative
        hostgroup_id = 1
        weight = 100
        max_connections = 1000
        status = "ONLINE"
        comment = "Server with invalid port"
    },
    {
        address = "127.0.0.2"
        port = 99999  # Invalid: port exceeds maximum (65535)
        hostgroup_id = 2
        weight = 100
        status = "ONLINE"
        comment = "Server with port too high"
    },
    {
        address = "127.0.0.3"
        port = 3306
        hostgroup_id = 3
        weight = -50  # Invalid: weight cannot be negative
        max_connections = 1000
        status = "ONLINE"
        comment = "Server with negative weight"
    },
    {
        address = "127.0.0.4"
        port = 3306
        hostgroup_id = 4
        weight = 100
        max_connections = 1000
        max_latency_ms = -100  # Invalid: latency cannot be negative
        status = "ONLINE"
        comment = "Server with negative latency"
    },
    {
        address = "127.0.0.5"
        port = 3306
        hostgroup_id = 5
        weight = 100
        max_connections = 1000
        max_replication_lag = -10  # Invalid: replication lag cannot be negative
        status = "ONLINE"
        comment = "Server with negative replication lag"
    }
)

mysql_users =
(
    {
        username = "root"
        password = "root"
        default_hostgroup = 1
        default_schema = "information_schema"
        backend = 1
        frontend = 1
    }
)
