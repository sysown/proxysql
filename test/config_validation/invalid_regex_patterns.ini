# Configuration file with invalid regex patterns
# This tests regex validation in query rules

admin_variables =
{
    admin_credentials = "admin:admin;radmin:radmin"
    mysql_ifaces = "127.0.0.1:6032"
}

mysql_variables =
{
    threads = 4
    max_connections = 2048
    interfaces = "0.0.0.0:6033"
    default_schema = information_schema
}

mysql_servers =
(
    {
        address = "127.0.0.1"
        port = 3306
        hostgroup_id = 1
        weight = 100
        max_connections = 1000
        status = "ONLINE"
        comment = "Primary server"
    }
)

mysql_users =
(
    {
        username = "root"
        password = "root"
        default_hostgroup = 1
        default_schema = "information_schema"
        backend = 1
        frontend = 1
    }
)

mysql_query_rules =
(
    {
        rule_id = 1
        active = 1
        match_pattern = "(?unclosed"  # Invalid: unclosed group
        destination_hostgroup = 1
        apply = 1
    },
    {
        rule_id = 2
        active = 1
        match_pattern = "[unclosed"  # Invalid: unclosed character class
        destination_hostgroup = 1
        apply = 1
    },
    {
        rule_id = 3
        active = 1
        match_pattern = "(?=.*invalid)"  # Valid regex
        destination_hostgroup = 1
        apply = 1
    },
    {
        rule_id = 4
        active = 1
        match_pattern = "SELECT.*FROM"  # Valid regex
        replace_pattern = "MATCH"  # Valid
        destination_hostgroup = 2
        apply = 1
    }
)
