name: CI-builds

on:
  push:
#    branches: [ "v2.7" ]
    paths-ignore:
    - '.github/**'
    - '**.md'
  pull_request:
#    branches: [ "v2.7" ]
    paths-ignore:
    - '.github/**'
    - '**.md'
#  schedule:
#    - cron: '15 13 * * 3'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  run:
    uses: mirostauder/proxysql/.github/workflows/ci-builds.yml@GH-Actions
    secrets: inherit
    
